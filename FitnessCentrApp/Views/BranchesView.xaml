<UserControl x:Class="FitnessCentrApp.Views.BranchesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:FitnessCentrApp.ViewModels" 
             xmlns:local="clr-namespace:FitnessCentrApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:BranchesViewModel />
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding Branches}"
                  SelectedItem="{Binding SelectedBranch}"
                  AutoGenerateColumns="True"
                  IsReadOnly="False" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Добавить" Command="{Binding AddCommand}" Margin="5"/>
            <Button Content="Обновить" Command="{Binding UpdateCommand}" Margin="5"/>
            <Button Content="Удалить" Command="{Binding DeleteCommand}" Margin="5"/>
            <Button Content="Обновить список" Command="{Binding RefreshCommand}" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
