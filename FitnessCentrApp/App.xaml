<Application x:Class="FitnessCentrApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FitnessCentrApp"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Конвертер для отображения плейсхолдера -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Универсальный стиль для TextBox с Placeholder -->
        <Style TargetType="TextBox" x:Key="PlaceholderTextBoxStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Само текстовое поле -->
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="false"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"
                                          Background="{TemplateBinding Background}"/>

                            <!-- Плейсхолдер -->
                            <TextBlock Text="{TemplateBinding Tag}"
                                       Foreground="Gray"
                                       Margin="5,2,0,0"
                                       IsHitTestVisible="False"
                                       FontStyle="Italic"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding Text.IsEmpty,
                                                    RelativeSource={RelativeSource TemplatedParent},
                                                    Converter={StaticResource BoolToVisibilityConverter}}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
